package com.soulboundlegacy.capability;\n\nimport net.minecraft.nbt.CompoundTag;\nimport java.util.HashMap;\nimport java.util.Map;\n\npublic class LegacyPointsCapability {\n    private long totalLegacyPoints = 0;\n    private Map<String, Long> playerLegacyPoints = new HashMap<>();\n    private Map<String, String> fallenSouls = new HashMap<>(); // UUID -> legacy text\n    private int deathCount = 0;\n    \n    public LegacyPointsCapability() {}\n    \n    // Dodaj Legacy Points igraču\n    public void addLegacyPoints(String playerName, long amount) {\n        totalLegacyPoints += amount;\n        playerLegacyPoints.put(playerName, \n            playerLegacyPoints.getOrDefault(playerName, 0L) + amount);\n    }\n    \n    // Konsumuj Legacy Points za aktivacije\n    public boolean consumeLegacyPoints(long amount) {\n        if (totalLegacyPoints >= amount) {\n            totalLegacyPoints -= amount;\n            return true;\n        }\n        return false;\n    }\n    \n    // Spremi Fallen Soul (spektralni otisak mrtvog igrača)\n    public void recordFallenSoul(String playerUUID, String playerName, long legacyPoints) {\n        deathCount++;\n        String legacy = String.format(\"%s - %d LP (Death #%d)\", playerName, legacyPoints, deathCount);\n        fallenSouls.put(playerUUID, legacy);\n        addLegacyPoints(playerName, legacyPoints / 10); // 10% LP za necromancer\n    }\n    \n    // Pribavi Legacy Points od igrača\n    public long getPlayerLegacyPoints(String playerName) {\n        return playerLegacyPoints.getOrDefault(playerName, 0L);\n    }\n    \n    // Pronađi Fallen Soul\n    public String getFallenSoul(String playerUUID) {\n        return fallenSouls.getOrDefault(playerUUID, null);\n    }\n    \n    public long getTotalLegacyPoints() {\n        return totalLegacyPoints;\n    }\n    \n    public int getDeathCount() {\n        return deathCount;\n    }\n    \n    // NBT serijalizacija\n    public CompoundTag serializeNBT() {\n        CompoundTag tag = new CompoundTag();\n        tag.putLong(\"totalLegacyPoints\", totalLegacyPoints);\n        tag.putInt(\"deathCount\", deathCount);\n        \n        // Spremi player points\n        CompoundTag playerPoints = new CompoundTag();\n        for (Map.Entry<String, Long> entry : playerLegacyPoints.entrySet()) {\n            playerPoints.putLong(entry.getKey(), entry.getValue());\n        }\n        tag.put(\"playerPoints\", playerPoints);\n        \n        // Spremi fallen souls\n        CompoundTag souls = new CompoundTag();\n        for (Map.Entry<String, String> entry : fallenSouls.entrySet()) {\n            souls.putString(entry.getKey(), entry.getValue());\n        }\n        tag.put(\"fallenSouls\", souls);\n        \n        return tag;\n    }\n    \n    public void deserializeNBT(CompoundTag tag) {\n        totalLegacyPoints = tag.getLong(\"totalLegacyPoints\");\n        deathCount = tag.getInt(\"deathCount\");\n        \n        CompoundTag playerPoints = tag.getCompound(\"playerPoints\");\n        playerLegacyPoints.clear();\n        for (String key : playerPoints.getAllKeys()) {\n            playerLegacyPoints.put(key, playerPoints.getLong(key));\n        }\n        \n        CompoundTag souls = tag.getCompound(\"fallenSouls\");\n        fallenSouls.clear();\n        for (String key : souls.getAllKeys()) {\n            fallenSouls.put(key, souls.getString(key));\n        }\n    }\n}\n